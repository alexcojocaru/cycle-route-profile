TODO
====

Disable the Google route directions while the elevations are being fetched.

do I stil need promises? If no, remove the polyfill from App.jsx and package.json

update the max route point count / split route point count in routeModifiers

reset the log levels in logger.js

catch the expired API key error and display a message in the UI
